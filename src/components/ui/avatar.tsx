import * as AvatarPrimitive from "@radix-ui/react-avatar";
import { cn } from "../../lib/cn";
export interface AvatarProps { src?: string; alt: string; name: string; online?: boolean; className?: string; }
export function Avatar({ src, alt, name, online, className }: AvatarProps) { const initials = name.trim().split(/\s+/).slice(0, 2).map(part => part[0]?.toUpperCase()).join(""); return <span className="relative inline-flex"><AvatarPrimitive.Root className={cn("inline-grid size-10 shrink-0 place-items-center overflow-hidden rounded-full bg-secondary", className)}><AvatarPrimitive.Image src={src} alt={alt} className="size-full object-cover"/><AvatarPrimitive.Fallback className="text-sm font-semibold" delayMs={100}>{initials}</AvatarPrimitive.Fallback></AvatarPrimitive.Root>{online !== undefined && <span className={cn("absolute bottom-0 right-0 size-3 rounded-full border-2 border-surface", online ? "bg-success" : "bg-muted-foreground")} aria-label={online ? "Online" : "Offline"}/>}</span>; }
